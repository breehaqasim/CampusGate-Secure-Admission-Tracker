# ZAP baseline rule overrides (see https://www.zaproxy.org/docs/docker/baseline-scan/ )
#
# Plugin 90004 (Site isolation / CORP–COEP–COOP): satisfied in nginx via
# Cross-Origin-Resource-Policy, Cross-Origin-Embedder-Policy: credentialless, and
# Cross-Origin-Opener-Policy (see nginx-csp.inc). Add rule lines here only if you need
# per-plugin overrides (tab-separated: pluginId<TAB>IGNORE|INFO|WARN|FAIL<TAB>comment).
